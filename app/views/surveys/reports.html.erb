<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>Revenue Grader - Survey Reports</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="">
        <meta name="author" content="">
        <link href='http://fonts.googleapis.com/css?family=Lato:400,300' rel='stylesheet' type='text/css'>
        </head> 
       
        <!-- Le styles -->
<%= content_for :main_content do%>
       <link rel="stylesheet" media="all" href="#{Rails.root}/assets/stylesheets/jquery.dataTables.css" />
        <%= render :partial=>"reports_css"%>
        <div class="content" style="page-break-after:always;">
            <h1>Revenue Grader</h1>
            <img src="/assets/logo.png" alt="logo">
            <h1> <%= "#{current_user.companies.first.name}"%></h1></br>
             <h1><%= @survey.created_at.strftime("%B  %Y")%></h1>
            <h1>Report 2012</h1>
            
        </div>

        <div class="content" style="page-break-after:always;">
            <div class="content2">
            <h2> <%= "#{current_user.companies.first.name}"%></h2>
            <h3><%= @survey.created_at.strftime("%B  %Y")%></h3>
            </div>
            <h1>Diagnostic Details</h1>
            <h2>Industry :: <%#= "#{current_user.companies.first.}"%></h2>
             <h2>Size ::<%#= "#{current_user.surveys.first.size}"%></h2>
            <h2>Revenue :: <%#= "#{current_user.surveys.first.revenue}"%></h2>
            <div class="footer">
              <img src="/assets/logo.png" alt="logo">  
            </div>
        </div>

        
         <div class="content" style="page-break-after:always;">
            <h2> <%= "#{current_user.companies.first.name}"%></h2>
          <div class="graph">
            <h1>Overall Diagnostic</h1>
                      <div id='chart'></div>
                      <%= render_chart(@chart, 'chart') %>
          </div>
          <div class="footer">
              <img src="/assets/logo.png" alt="logo">  
            </div>
         </div>

        <div class="content" style="page-break-after:always;">
            <div class="content2">
            <h2> <%= "#{current_user.companies.first.name}"%></h2>
            <h3><%= @survey.created_at.strftime("%B  %Y")%></h3>
            </div>
             <h1>Must Do</h1>
             <div class="textbox">
              <table cellpadding="1" cellspacing="1" 
                      id="report_add_to_planTable" class="report-body dataTable2">   
                       <table cellpadding="1" cellspacing="1" id="report_must_doTable" class="report-body dataTable2"> 
                            <%= render :partial=>"table_header"%>
                            <% @must_do_responses.each do |response|%>
                              <%= render :partial=>"response_report", 
                              :locals=> {:response =>response} %> 
                            <%end%>
                           <%= render :partial=>"table_end"%>
                    </div>        
            </div>
            <div class="footer">
              <img src="/assets/logo.png" alt="logo">  
            </div> 
        </div>  
       
        <div class="content" style="page-break-after:always;">
         <div class="content2">
            <h2> <%= "#{current_user.companies.first.name}"%></h2>
            <h3><%= @survey.created_at.strftime("%B  %Y")%></h3>
            </div>
             <h1>Should Do</h1>
             <div class="textbox">
             <table cellpadding="1" cellspacing="1" id="report_should_doTable" class="report-body dataTable2"> 
               <%= render :partial=>"table_header"%>
                <% @should_do_responses.each do |response|%>
                  <%= render :partial=>"response_report", 
                  :locals=> {:response =>response} %> 
                <%end%>
               <%= render :partial=>"table_end"%>           
            </div> 
            <div class="footer">
              <img src="/assets/logo.png" alt="logo">  
            </div>
        </div>  

       
        <div class="content" style="page-break-after:always;">
            <div class="content2">
            <h2> <%= "#{current_user.companies.first.name}"%></h2>
            <h3><%= @survey.created_at.strftime("%B  %Y")%></h3>
            </div>
             <h1>Could Do</h1>
             <div class="textbox">
             <table cellpadding="1" cellspacing="1" id="report_could_doTable" class="report-body dataTable2">
                    <%= render :partial=>"table_header"%>
                    <% @could_do_responses.each do |response|%>
                      <%= render :partial=>"response_report", 
                      :locals=> {:response =>response} %> 
                    <%end%>
                   <%= render :partial=>"table_end"%>            
            </div> 
            <div class="footer">
              <img src="/assets/logo.png" alt="logo">  
            </div>
        </div> 

        <div class="content" style="page-break-after:always;">
            <div class="content2">
            <h2> <%= "#{current_user.companies.first.name}"%></h2>
            <h3><%= @survey.created_at.strftime("%B  %Y")%></h3>
            </div>
             <h1>Items not applicable to your company</h1>
             <div class="textbox">
             <table cellpadding="1" cellspacing="1" id="report_could_doTable" class="report-body dataTable2">
                    <%= render :partial=>"table_header"%>
                    <% @not_applicable_responses.each do |response|%>
                      <%= render :partial=>"response_report", 
                      :locals=> {:response =>response} %> 
                    <%end%>
                   <%= render :partial=>"table_end"%>            
            </div> 
            <div class="footer">
              <img src="/assets/logo.png" alt="logo">  
            </div>
        </div> 

        <div class="content" style="page-break-after:always;">
            <div class="content2">
            <h2> <%= "#{current_user.companies.first.name}"%></h2>
            <h3><%= @survey.created_at.strftime("%B  %Y")%></h3>            </div>
             <h1>Items already planned for your company</h1>
             <div class="textbox">
             <table cellpadding="1" cellspacing="1" id="report_could_doTable" class="report-body dataTable2">
                    <%= render :partial=>"table_header"%>
                    <% @in_plan_responses.each do |response|%>
                      <%= render :partial=>"response_report", 
                      :locals=> {:response =>response} %> 
                    <%end%>
                   <%= render :partial=>"table_end"%>            
            </div> 
            <div class="footer">
              <img src="/assets/logo.png" alt="logo">  
            </div>
        </div> 

        <div class="content" style="page-break-after:always;">
            <div class="content2">
            <h2> <%= "#{current_user.companies.first.name}"%></h2>
            <h3><%= @survey.created_at.strftime("%B  %Y")%></h3>
            </div>
             <h2>About Revenue Grader</h2>
             <p class="aboutrevenue">Lorem ipsum dolor sit amet, consectetur adipiscing elit.
          Aenean eu erat et velit dapibus bibendum id quis quam.
          Sed quis suscipit tortor.</p>
          <div class="footer">
              <img src="/assets/logo.png" alt="logo">  
            </div>
        </div>
<%end%>
   
    </body>
</html>


          