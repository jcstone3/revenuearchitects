<!DOCTYPE html>
<html lang="en">
  <head>
    <link rel="shortcut icon" href="assets/rg_icon.png">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>RevenueGrader</title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->
        <link href='http://fonts.googleapis.com/css?family=Lato:400,300' rel='stylesheet' type='text/css'>
                <link href='http://fonts.googleapis.com/css?family=Poiret+One' rel='stylesheet' type='text/css'>
    <!-- Le styles -->
    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- Le fav and touch icons -->
    <!--<link href="images/favicon.ico" rel="shortcut icon"> -->
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body>
    <div class="container mainLeft" >
          <% if user_signed_in?%>
          <%= link_to image_tag('logo.png', :alt=>"logo"),  "", :class=>"brand" %>
          <div class="ra">
            brought to you by<%= link_to image_tag("ra_logo.png", :alt=>"ra_logo", :class=>"headertext"), "" %>
          </div>
          <%else%>
        
          <%= link_to image_tag('logo.png', :alt=>"logo"),  new_user_session_path, :class=>"brand" %>
          <div class="ra">
            brought to you by<%= link_to image_tag("ra_logo.png", :alt=>"ra_logo", :class=>"headertext"), new_user_session_path %>
          </div>
          <%end%>
          


            <div class="button-bar">
             <div class= "useralign">
             <% if user_signed_in?%>
              <i class="icon-user " style="font-size:20px"></i>&nbsp; 
              <%= "#{current_user.username}" %>&nbsp;<br/>
              <% if current_user.companies.present? %>
              <i class="icon-home " style="font-size:20px"></i>&nbsp;
               <%= "#{current_user.companies.first.name}"%>
               <%end%> </div>
               <div class="userbtn">              
              <%= link_to "Logout", destroy_user_session_path, :class=>"btn" %>
              <%else%>

               <%= link_to "Home", root_url, :class=>"header_link" %>
               <%= link_to "About Us", aboutus_url, :class=>"header_link" %>
               <%= link_to "Contact Us", contactus_url, :class=>"header_link" %>
              

              <%= link_to "Login", new_user_session_path, :class=>"btn btn-primary", :id=>"login_user"%>
              <%= link_to "Sign Up", new_user_registration_path, :class=>"btn btn-primary", :id=>"signup_user"%>
              <%end%></div>

            </div>
          </div>
        </div>
    <div class="container headerPopup" >
       <div id="alert">
       <% flash.each do |key, value| %>
             <%= content_tag :div, :id=>'flash_msg', :class => "alert alert-#{key}" do %>
             <%= value unless value.blank? %>                         
             <a class="close" data-dismiss="alert">×</a>
              
           <% end %>
        <% end %>   
        </div>       
        <%= yield%>
     </div>
    <%= render '/layouts/shared/footer'%>

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
   
    <%= javascript_include_tag "application" %>


  </body>
</html>
