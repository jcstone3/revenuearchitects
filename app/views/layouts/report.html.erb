<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Revenue Grader - Sign Up</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">


    <!-- Le styles -->

    <%#= stylesheet_link_tag "bootstrap", "report",:media => "all" %>
       <%= stylesheet_link_tag "application", :media => "all" %>
  
    <%#= stylesheet_link_tag  "bootstrap-responsive"   %>  
  </head>

  <body>
    <%#= render "/layouts/shared/header" %>
     <div class="container" style="background-color:#fff;">
          <%= link_to image_tag('logo.png', :alt=>"logo", :style=>"vertical-align: top; margin-left:10px;"),  show_url, :class=>"brand" %>
          <div class="ra">
            brought to you by<%= link_to image_tag("ra_logo.png", :alt=>"ra_logo", :style=>"vertical-align: top; margin:-4px 0 0 4px;"), show_url %>
          </div>
          <div class="row-fluid">
            <div class="button-bar">
             <% if user_signed_in?%>
              <%= "#{current_user.username}" %>
              <%= link_to "Logout", destroy_user_session_path, :class=>"btn" %>
              <%else%>
              <%= link_to "Login", new_user_session_path, :class=>"btn", :id=>"login_user"%>
              <%= link_to "Sign Up", new_user_registration_path, :class=>"btn btn-primary", :id=>"signup_user"%>
              <%end%>
            </div>
          </div>
        </div>
    <div class="container">
    <div id="alert">
       <% flash.each do |key, value| %>
             <%= content_tag :div, value, :class => "alert alert-#{key}" unless value.blank? %>
           <% end %>
        </div>
    </div>
    <%= yield%>   
    <%= render "/layouts/shared/footer" %>
    
    <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
     
    
    <%= javascript_include_tag "application"%>
       
    <script>
        $(document).ready(function(){
            
    $(".pageAccordion").accordion({
    autoHeight: false,
    navigation: true
    });
    }); 
        
    </script>

       <script>
    $(function() {
    //$('#default').raty();
    $('#fixed').raty({
    readOnly: true,
    start: 3
    });
    }); 
    </script> 
        
  </body>
</html>
